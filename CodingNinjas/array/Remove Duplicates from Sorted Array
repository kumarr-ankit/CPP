#include<set>
int removeDuplicates(vector<int> &arr, int n) {
	// Write your code here.
	//sorted--> two pointer
	if(n==1) return 1;

	int p = 0;
	int q = 1;
	int c =1;
	while(q<n){
		if((arr[q] != arr[p])&& ((p+1)<n) ){

			arr[p+1] = arr[q];
			p++;
			c++;

		}
		q++;
	}


return c;
 
}
