class Solution {
public:
    int single(vector<int>& nums) {

        int sum = 0;

        for (int x : nums) {

            sum = sum ^ x;
        }
        return sum;
    }
    int singleNumber(vector<int>& nums) {
        int ans = single(nums);
        return ans;
    }
};
