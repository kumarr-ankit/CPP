class Solution {
public:
    void reverseString(vector<char>& s) {

        int p = 0;
        int l = s.size()-1;

        while(p<l){

            swap(s[p],s[l]);
            p++;
            l--;
        }
        
    }
};
