    ListNode* solve(ListNode* head){
    
     if(!head || !head->next) return head;
     ListNode* first = head;
     ListNode* sec = head->next;

     first->next = solve(sec->next);
     sec->next = first;
     return sec;

    }
